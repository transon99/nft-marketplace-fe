"use client";

import FormWrap from "@/components/FormWrap";
import LoginForm from "./LoginForm";

const RegisterPage = () => {
  return (
    <FormWrap>
      <LoginForm />
    </FormWrap>
  );
};

export default RegisterPage;
